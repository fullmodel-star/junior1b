# 更新記錄 — 國一下複習（康軒內部版）

> 線上版：<https://fullmodel-star.github.io/junior1b/>

---

## 2026-07-11 — 家長雲端後台回報

- **家長後台（雲端）**：新增 engSync 回報模組，本 App 進度自動上傳到自家 Cloudflare Worker，家長一個網址即可看兩個小孩各科進度。回報本 App 專屬指標（總正確率、精熟、累積作答、待清錯題、已征服），沒有的指標後台顯示「—」。
  - 只讀明文 `kh1b_v2`（不碰加密的 DB），App 鎖著也能算，不影響題庫加密。
  - 已同時注入 `scripts/template.html`，重建 index.html 不會掉。
- 順帶修正 `_install_inject.py` 注入區塊漏 `</script>` 的老問題（本 App 受影響，已修）。
- sw 快取升版（`kh1b-…-sync1`）讓已安裝裝置更新。
- 注入器：`_parentsync_inject.py`；後台在 `_家長後台/`。
